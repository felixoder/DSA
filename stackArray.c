//Implementing Stack using Array in Heap
#include<stdio.h>
#include<stdlib.h>
struct stact
{
    int size;
    int top;
    int *arr;
};

int main(){
    struct stact *s;
    s->size = 80;
    s->top = -1;
    s->arr = (int *)malloc(s.size* sizeof(int));




    return 0;
}